services:
  cleva-amazon-intelligence:
    build: .
    restart: unless-stopped
    ports:
      - "8501:8501"
    env_file:
      - .env
    environment:
      LOCAL_DATA_DIR: /data
    volumes:
      - ./data:/data
